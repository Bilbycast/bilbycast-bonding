[package]
name = "bonding-protocol"
version = "0.3.0"
edition = "2024"
description = "Pure Rust packet-bonding protocol (media-aware multi-path aggregation for broadcast)"
license = "MPL-2.0"
publish = false

[dependencies]
bytes = "1.11"
log = "0.4"
thiserror = "2.0"
